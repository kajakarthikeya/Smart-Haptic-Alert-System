# Log File Storage Directory (`app/logs/`)

## Purpose
Stores application execution log files generated by `app.utils.logger`.

## Log Files
- `app.log`: Rotating application execution log (max 5MB, up to 3 backups).

> [!NOTE]
> All `.log` files in this directory are excluded from Git repository commits via `.gitignore`.
