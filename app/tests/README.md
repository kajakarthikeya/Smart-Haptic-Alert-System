# Test Suite Subsystem (`app/tests/`)

## Purpose
Contains unit and integration tests written using `pytest` and `pytest-asyncio` verifying system settings, context prioritization, and alert dispatches.

## Running Tests

Execute tests from the project root directory:

```bash
pytest
```

To run with coverage report:

```bash
pytest --cov=app
```
